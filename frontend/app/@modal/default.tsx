/** Niets in het modal-slot bij een gewone paginanavigatie. Zonder deze `default` faalt een
 *  refresh op een pad dat het slot niet vult (werkwijze-story 042). */
export default function ModalDefault() {
  return null;
}
