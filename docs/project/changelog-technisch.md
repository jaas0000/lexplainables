# Technische changelog

- Bijlagen voor bwb-import (PR #64, story 033): poort van `wetsanalyse-ai`'s `_parse_bijlage`/`_bijlage_tekst`. Een bijlage (kind van `<wet-besluit>`/`<regeling>`, ná de wettekst) is container (eigen artikelen/onderdelen) én tekstdrager, citeerbaar op JuriConnect-sleutel net als een artikel. Hergebruikt de bestaande provenance-/voetnoot-/definitie-/illustratie-/tabel-logica uit story 031 en de artikel-traversal in `collect.py`. Eerste documentvolgorde-relatie in het project (`VOLGT_OP` tussen opeenvolgende bijlagen). Handmatig geverifieerd tegen de lokale, gelicentieerde GraphDB met een synthetische `Wet` (de Invorderingswet-fixture bevat geen bijlage).
- Wet-brondata/aanhef/considerans/ondertekenaars voor bwb-import (PR #63, story 032): poort van `wetsanalyse-ai`'s `_wet_brondata`/`_wet_aanhef`/`_parse_ondertekenaars`. `Wet` draagt nu publicatiebron (Stb-jaar/nummer, ondertekenings-/uitgiftedatum, Kamerdossier), aanhef/considerans, toestand-URL (`bwb-ng-vast-deel`) en ondertekenaars. Ondertekenaars zijn wet-overstijgende, open-world-gededupliceerde nodes (zelfde patroon als de WTI-Organisatie-node uit story 030). `collect.py` uitgebreid om de brondata-velden op de Regeling-node-props te zetten. Handmatig geverifieerd tegen de lokale, gelicentieerde GraphDB.
- Artikel/lid/onderdeel-verrijking voor bwb-import (PR #62, story 031): poort van `wetsanalyse-ai`'s provenance-attributen (inwerking/bron/effect/status/terugwerkend_tot/wijzigingsbronnen), voetnoten, cursief-gedefinieerde begrippen en illustraties naar `parser.py`/`collect.py`/`ontology.py`/`models.py`. CALS-tabellen worden niet langer uitgesloten van de lopende tekst maar als leesbare `cel | cel`-rijen gerenderd (`_tabel_tekst`). Nieuwe `Illustratie`-node + `BEVAT_ILLUSTRATIE`-relatie loopt mee via de bestaande generieke node-/rel-schrijflus in `graphdb_writer.py` (geen signatuurwijziging). Handmatig geverifieerd tegen de lokale, gelicentieerde GraphDB.
- WTI-verrijking voor bwb-import (PR #61, story 030): poort van `wetsanalyse-ai`'s `wti_parser.py` + WTI-secties (downloader/config/main/graphdb_writer/ontology/rdf_vocab) naar `tools/bwb-import`, aangepast aan dit projects eenvoudigere node-schema (geen `iri_soort`/`iri_sleutel`). Nieuwe `WtiParser`/`WtiInfo`/`WtiElementRel`, `Vocab.begrip()`/`Vocab.entiteit()`, ontologie-uitbreiding (`Organisatie`-klasse, `heeftGrondslag`/`uitgegevenDoor`/`inFamilie`/`grondslagVoor`/`bevoegdheidVoor`/`verwijzingDoor`), `label_id`-join-sleutel op `Wet`/`Structuurdeel`/`Artikel`. Opt-in via `BWB_IMPORT_WTI` (default uit), best-effort (een falende WTI-stap breekt de kernwet-import nooit). Handmatig geverifieerd tegen de lokale, gelicentieerde GraphDB.
- docs(c4-model): bijwerken t/m PR #27 — L2-container-tekst voor `api` en `frontend` reflecteert nu de gebouwde stand; L3-`api` uitgebreid met alle features (annotatie, api_tokens, llm_calls, llm_profielen, projecten, runtime_config, wetcatalogus), engine-module en shared-modules (wettenbank, llm, db, crypto, validation, tijd) inclusief relaties; L3-`frontend` uitgebreid met alle pagina's (account, setup, beheer-subpagina's, wetcatalogus, projecten/detail, werkplek/lijst+detail, disclaimer) en de `apiProxy` + BFF-groep. Adresseert de 8 opgestapelde c4-model-vervolgpunten uit PRs #9, #10, #11, #17, #18, #21, #22, #27.
- refactor(llm_calls): verhuizen naar eigen feature-map (PR #27): `llm_calls`-tabel, `LlmCallRead` en `SqlAlchemyLlmCallsStore` verplaatst uit `projecten/` naar de nieuwe `api/app/features/llm_calls/`-feature (models/store/dependencies/tests), met eigen `MetaData()`-instantie. Endpoint `GET /v1/projecten/{analyse_id}/llm-calls` blijft in projecten (path-scoped resource) maar importeert `LlmCallRead` + `SqlAlchemyLlmCallsStore` + `get_llm_calls_store` uit de nieuwe locatie; `engine/orchestrator.py` idem (lazy import weg — geen cycle meer). `alembic/env.py` neemt `llm_calls_metadata` op in de autogenerate-lijst. Adresseert punt 4 uit architectuur-audit ronde 2. Simplify: `get_llm_calls_store` naar `llm_calls/dependencies.py` (weg uit projecten's router-owner-export), lazy import in orchestrator opgeheven, ongebruikte `Integer`-import uit `projecten/models.py` verwijderd, `AsyncEngine`-typing in `SqlAlchemyLlmCallsStore.__init__`.
- refactor(auth): DB-verify verhuizen naar api_tokens (owner-export) (PR #26): shared/auth.py roept nu de nieuwe publieke owner-export verifieer_db_token uit api_tokens.store aan; lazy import + try/except in shared/ verwijderd, boundary shared/ → features/ hersteld (audit ronde 2, punt 3).
- Shared upsert-helper voor berichten en runtime_config (PR #24): Ontdubbelt de dialect-aware upsert-boilerplate (`pg_insert if is_pg else sqlite_insert`) naar `api/app/shared/db.py` met twee helpers — `dialect_insert(engine_or_conn, table)` voor callers die zelf `.from_select()`/`.on_conflict_*()` chainen (berichten.markeer_alles_gelezen) en `upsert(engine_or_conn, table, values, conflict_cols, update_cols=None)` voor de complete upsert (runtime_config.schrijf, wetcatalogus.upsert). `wetcatalogus.upsert` gaat tegelijk van check-then-insert (2 queries, TOCTOU-race op Postgres) naar één atomaire upsert met `RETURNING` — lost MEDIUM-vervolgpunt uit PR #15 op. 5 unit-tests in `api/app/shared/tests/test_db.py` dekken insert/update-pad + dialectkeuze. Adresseert punt 2 uit architectuur-audit ronde 2.
- Wettenbank-MCP JSON-RPC-consolidatie (PR #23): Ontdubbelt de JSON-RPC POST-boilerplate tussen `shared/wettenbank.haal_artikel_op` en `wetcatalogus/router._roep_wettenbank_mcp_aan`. Nieuwe private `_jsonrpc_call(method, arguments, foutcontext)` doet de gedeelde plumbing (payload, POST, netwerkfouten, HTTP-status, JSON-RPC-error, content-blokken). Nieuwe publieke `haal_citeertitel_op(bwb_id) -> str` (tool `wettenbank_structuur`). Twee subklassen `WettenbankNietBereikbaar` en `WettenbankNietGevonden` zodat de router 502/404 kan blijven onderscheiden. Router-boilerplate (~75 regels) verwijderd. Adresseert punt 1 uit architectuur-audit ronde 2.
- Werkplek annotatie-UI (PR #22, story 023): BFF-proxy-routes voor alle annotatie-endpoints van story 022 (documenten GET/POST/DELETE, elementen PUT, beslissing POST, auditlog GET) via `apiProxy()` + `requireSession()`; werkplek-pagina `/werkplek/` (documentenlijst met status-badges, aanmaakformulier, `VerwijderKnop`); documentdetailpagina `/werkplek/{slug}` met twee-kolommen-layout (wetsartikeltekst-placeholder links, elementenlijst rechts), beslissingsacties (goedkeuren/bewerken/afwijzen) met verplichte reden-select, auditlog-tabblad; `NavigatieHeader.tsx` uitgebreid met Werkplek-link; `generated/types.ts` geregenereerd inclusief annotatie-types; Playwright E2E-tests voor lijst, aanmaken, beslissingen, auditlog en verwijderflow. Simplify: `formatDatum` geëxtraheerd naar `lib/datum.ts`; `VerwijderKnop` hergebruikt i.p.v. inline `confirm()`; unified `AANDACHT_STIJL`-lookup; `useMemo` voor sortering; redundante guard en dode stijl verwijderd; 401-redirect op alle mutaties.
- Annotatie-backend (PR #21, story 022): Migratie 0011 (`annotatie_documenten` + `annotatie_audit`), 7 endpoints onder `/v1/annotatie/documenten` (aanmaken, lijst, ophalen, verwijderen, bulk PUT elementen, beslissing registreren, auditlog opvragen), client-scoping via `X-User-Id` (andermans document → 404), JAS-klassevalidatie + lege-tekst-filter bij PUT (aanvaard/verworpen-count in response), levenscyclus- en documentstatus-herberekening na beslissing, append-only auditlog, 19 gedragstests. `GELDIGE_JAS_KLASSEN` verplaatst van `engine/validation.py` naar `shared/validation.py` (tweede onafhankelijke gebruiker; `engine/validation.py` herexporteert voor backwards-compatibility).
- LLM-calls log (PR #20, story 021): Voegt `GET /v1/projecten/{analyse_id}/llm-calls` toe + `SqlAlchemyLlmCallsStore.lijst_calls()` + admin-pagina `/beheer/llm-calls` voor het bekijken van vastgelegde LLM-aanroepen per analyse; BFF-route `frontend/app/api/projecten/[id]/llm-calls/route.ts`; Playwright E2E-test (`llm-calls.spec.ts`, 3 scenario's); 5 pytest-integratietests.
- API-tokens (PR #18, story 018): Voegt `api/app/features/api_tokens/` toe — SQLAlchemy Core-tabel, Protocol+SqlAlchemy-store, drie admin-endpoints (`GET`/`POST`/`DELETE /v1/admin/api-tokens`), Alembic-migratie 0010. `shared/auth.py` async gemaakt en uitgebreid met DB-tokenverificatielaag (SHA-256, `hmac.compare_digest` voor statisch token). Frontend: `/beheer/api-tokens`-pagina met eenmalig-token-modal en `/beheer`-badge met actief-tokenaantal.
- Rapport bekijken (PR #19, story 013): `GET /v1/projecten/{id}/rapport` (JSON) + `GET /v1/projecten/{id}/rapport.md` (Markdown-download met `Content-Disposition`-header); `rapport_md.py` defensieve Markdown-renderer (werkgebied, bronnen, begrippen, afleidingsregels); frontend-pagina `/projecten/{id}/rapport` met bronnen-kaarten, begrippen-tabel en afleidingsregels-lijst, teruglink + download-knop; 409-melding als analyse nog niet klaar; `apiProxy` uitgebreid met optionele `forwardHeaders`-parameter; Playwright E2E-test (409 + 404); 7 gedragstests.
- Analyse-engine (PR #17, story 024): `api/app/engine/` (orchestrator act2→human-in-the-loop→act3, steps, prompts, retry met exponential backoff), `api/app/shared/llm/` (LLMClient Protocol + LiteLLMClient met JSON-parse-herpoging), `api/app/shared/wettenbank.py` (JSON-RPC POST naar Wettenbank-MCP), `api/app/shared/validation.py` (JAS-klasscheck + brongetrouwheid NFKC-substring), migratie 0009 (`analyses.rapport JSON nullable` + `llm_calls`-tabel), nieuwe endpoints `POST /v1/projecten/{id}/akkoord` + `POST /v1/projecten/{id}/afwijzen` (human-in-the-loop signalering), `api/references/` (jas-klassen-referentie + begrippen), LLM-capture via `sla_llm_call_op` + `capture_llm_calls` runtime_config-toggle, twee nieuwe store-methoden (`haal_rij_op_naam` + `haal_standaard_rij`) op `llm_profielen`; 157 tests groen, ruff clean.
- Initiële API-features (PR #1, stories 001-002): `api/app/features/feedback/` (indienen, admin-lijst, verwijderen, ongelezen-aantal, markeer-gezien) en `api/app/features/berichten/` (aanmaken, bewerken, publiceren/depubliceren, verwijderen admin; lezen, ongelezen-status, lees-alles analist); `api/app/features/identiteit_toegang/` (eigen gebruikers-tabel, bcrypt, POST /v1/auth/verify); SQLAlchemy Core + Pydantic als "ene bron", Alembic-migraties, pytest-integratietests.
- Eerste frontend: berichten-admin-scherm (PR #2): Eerste frontend-service (`frontend/`) — Next.js admin-scherm voor berichtenbeheer, contractgeneratie API→TypeScript via `openapi-typescript`, CORS-middleware op de API, Playwright E2E-test (gelukkig pad + foutpad), eigen `frontend-ci.yml`, C4-architectuurdocumentatie.
- Huisstijl-frontend (PR #3): Wetsanalyse-kleurtokens als CSS-variabelen in `globals.css`, Fira Sans via `next/font/google`, navigatieheader in `layout.tsx`, en herstructurering van `page.tsx` naar CSS-klassen (`btn`, `field-input`, `tabel`, `badge`, `melding`); E2E-selectorfix `p[role="alert"]` in commit `9c45f09`.
- Auth — Keycloak OIDC-login (PR #4, story 005): Keycloak Authorization Code + PKCE-flow in de frontend (SubtleCrypto, localStorage), JWT-verificatie via JWKS in de API (RS256, cache TTL 300s, asyncio.Lock), `shared/auth.py` vervangt X-Admin-Id header-stand-in door `huidige_beheerder`-dependency met `GebruikerContext`, Playwright E2E via ROPC + `page.addInitScript`, realm-export + CI-opstapstap voor Keycloak 26.
- Auth — eigen gebruikers (PR #5, story 006): Vervangt Keycloak OIDC door eigen `gebruikers`-tabel (bcrypt, SQLModel, Alembic-migratie 0003), `POST /v1/auth/verify` achter API_TOKEN-gate met constant-time vergelijking en timing-oracle-bescherming, Auth.js v5 Credentials provider met httpOnly-sessiecookie, Next.js BFF-routes voor admin-endpoints (`/api/admin/berichten/...`), Keycloak volledig verwijderd uit docker-compose en CI.
- Disclaimer-gate + PoC-strip (PR #7, story 008): server-side middleware-redirect op basis van httpOnly-cookie (`disclaimer_geaccepteerd`), `/disclaimer`-pagina met drie waarschuwingsblokken, PoC-strip in `layout.tsx`, `isDisclaimerExempt()` parallelt `isPublic()`; open-redirect-fix (route geeft gesaniteerde redirect-URL terug); simplify: `TYPE_META`/`TypeBadge`/`SectieHeader`/`LeegePlaceholder` geëxtraheerd naar gedeelde modules, mutatie-state in `beheer/page.tsx` werkt lokaal bij zonder extra GET; favicon + `manifest.webmanifest` toegevoegd.
- Feedback-frontend (PR #8, story 009): zwevende `FeedbackKnop` in `layout.tsx` voor alle ingelogde gebruikers; 5 BFF-routes (`POST /api/feedback`, GET/DELETE/markeer-gezien/ongelezen-aantal voor admin); admin-feedbackpagina (`/beheer/feedback`) met verwijderen + markeer-gezien op mount + 404-race-afhandeling; navigatieknop met ongelezen-badge op `/beheer`. Gedeelde modules: `lib/feedback-types.ts`, `lib/beheer-fetch.ts` (typed `BeheerFetchFout`, vervangt duplicaat in `beheer/page.tsx`), `components/feedback/CategorieBadge.tsx`, `components/feedback/FeedbackItem.tsx`.
- LLM-profielen (PR #10, story 011): `LlmProfielenStore` (Protocol + SQLAlchemy-implementatie) + `shared/crypto.py` (Fernet-encryptie, fail-closed); CRUD-endpoints `GET/POST /v1/admin/profielen` + `PUT/DELETE /v1/admin/profielen/{naam}` achter `huidige_beheerder`; Alembic-migratie `0004_llm_profielen_tabel`; BFF-routes `app/api/admin/profielen/`; beheer-UI `/beheer/llm-profielen` (tabel + aanmaken-formulier + bewerk-/verwijder-actie per profiel); 15 integratietests + 3 Playwright E2E-scenario's; mockup-pagina gepromoveerd en verwijderd.
- Wetcatalogus (PR #9, story 010): Voegt wetcatalogus-feature toe aan `api` (`models.py`, `store.py`, `router.py`, 7 gedragstests; endpoints `GET /v1/wetten` + `GET /v1/wetten/{bwb_id}/structuur`, statische PoC-data zonder DB-tabel) en `frontend` (BFF-routes `app/api/wetten/` en `app/api/wetten/[bwbId]/structuur/`, herbruikbaar `WetSelector`-component met wet- + artikelkeuze, pagina `app/wetcatalogus/`, E2E-test gelukkig pad + foutpad); contractgeneratie bijgewerkt (`openapi.json`, `types.ts`); mockup-pagina gepromoveerd.
- Analyse aanmaken & volgen + SSE (PR #11, story 012): `api/app/features/projecten/` — SQLAlchemy Core `analyses`-tabel, Protocol-store (`AnalyseStore`/`SqlAlchemyAnalyseStore`), CRUD-endpoints + SSE-stroom (`GET /v1/projecten/{id}/events`) + BackgroundTasks-PoC-job (statusovergangen wachtrij→actief→review/klaar/fout), Alembic-migratie 0005, 15 pytest-integratietests; BFF-routes `app/api/projecten/` (GET/POST, GET/DELETE/{id}, GET/{id}/events/), pagina's `/projecten`, `/projecten/nieuw`, `/projecten/[id]`, gedeelde `StatusPill`/`VerwijderKnop`-components (`components/projecten/`), Playwright E2E-test (gelukkig pad + validatiefout), Tailwind CSS + PostCSS + autoprefixer toegevoegd; mockup `/mockup/analyse/` verwijderd (gepromoveerd); `buildBackendHeaders` geëxporteerd vanuit `lib/api-client.ts`.
- Setup-flow initiële beheerder (PR #13, story 015): `GET /v1/auth/setup-status` + `POST /v1/auth/setup` achter `vereist_api_token`; Alembic-migratie 0006 (`email TEXT NOT NULL DEFAULT ''` op `gebruikers`); `SetupStatus`/`SetupVerzoek`/`GebruikerInfo` Pydantic-modellen met gebruikersnaam-regex-validatie; frontend `/setup`-pagina met `SetupFormulier` (client component) + BFF-routes via `publiekApiProxy`; `lib/setup-status.ts` roept backend rechtstreeks aan (geen loopback); `auth.config.ts` maakt `/setup` publiek en voegt redirect-logica toe (ingelogd → `/`, setup al klaar → `/login`); E2E-test voor gelukkig en foutpad; mockup-pagina gepromoveerd.
- Account-pagina (PR #12, story 016): Voegt GET /v1/auth/me en POST /v1/auth/wijzig-wachtwoord toe aan de identiteit_toegang-feature (MijnProfiel + WachtwoordWijzigenVerzoek Pydantic-modellen, domain-exceptions GebruikerNietActief/WachtwoordOnjuist, bcrypt buiten de DB-sessie); BFF-routes app/api/auth/me en wijzig-wachtwoord; account-pagina (/account) met profielweergave en wachtwoord-formulier (inline foutmelding per veld, formulier-reset na succes); Account-link in NavigatieHeader geactiveerd; E2E-test (4 scenario's); auth-testfixture hersteld naar tmp SQLite-DB (fix pre-existing + nieuwe CI-fout).
- Gebruikersbeheer uitbreiden (PR #14, story 014): `admin_router` met GET/POST `/v1/admin/gebruikers`, PATCH/DELETE `/v1/admin/gebruikers/{naam}`, POST `/v1/admin/gebruikers/{naam}/reset-wachtwoord` (achter `huidige_beheerder`); LaatsteBeheerder-invariant (409 bij deactiveren/degraderen/verwijderen van de enige actieve beheerder); `GebruikerRead`, `GebruikerCreate`, `GebruikerPatch`, `TijdelijkWachtwoord` in `openapi.json` en `types.ts`; BFF-routes `app/api/admin/gebruikers/`; `/beheer/gebruikers`-pagina (gepromoveerd van mockup) + navigatieknop op `/beheer`; E2E-test gelukkig pad + foutpad (laatste beheerder 409); `maak_gebruiker_admin`-TOCTOU-fix (check+insert in één transactie).
- Wettenbank-beheer (PR #15, story 020): Vervangt `HardgecodeerdeWetcatalogusStore` door `DatabaseWetcatalogusStore` (SQLAlchemy Core) met Alembic-migratie 0007, voegt admin-CRUD-endpoints toe voor de wetcatalogus (GET lijst, PUT upsert, DELETE, POST resolve via httpx naar Wettenbank-MCP) en koppelt een beheerpagina `/beheer/wetten/` met inline bewerken, resolve-knop en verwijderen met bevestiging; contractgeneratie bijgewerkt (`openapi.json`, `types.ts`); seed van de 3 bestaande hardgecodeerde wetten in de migratie.
- App-instellingen / runtime-config (PR #16, story 019): nieuwe feature `api/app/features/runtime_config/` met tabel `app_instellingen` (migratie 0008), in-process TTL-cache (10s), `GET/PUT /v1/admin/instellingen` (beheerder-auth); dialect-aware upsert (sqlite + postgresql); frontend `/beheer/instellingen/` met capture-LLM-calls-toggle + statusbadge; mockup-pagina gepromoveerd en verwijderd; contractgeneratie bijgewerkt (`openapi.json`, `types.ts`).
- Admin-MCP (PR #6, story 007): Nieuwe TypeScript stdio MCP-server (`tools/wetsanalyse-admin-mcp/`) met vier tools (`list_berichten_admin`, `maak_bericht`, `update_bericht`, `publiceer_bericht`) die de lexplainables admin-API aansturen; auth via `Authorization: Bearer` + `X-User-Id`; vier integratietests met gemockte fetch; `dist/index.js` meegecommit; registreerbaar in `.mcp.json`.
