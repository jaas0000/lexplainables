# Technische changelog

- graph-qa annotatieketen afronden — patch/herziening/emit/graaf-wiring (PR #87, story 049):
  slotstory van de annotatieketen-werkstroom. `patch_node` (code-only, rood+vervang), `herzie_node`
  (LLM-call, herstelt verworpen fragmenten/gemiste elementen), `emit_node` (finale structuur, geen
  SSE). `state["doel"]` routeert via `_heeft_doel` om de supervisor heen naar de annotatieketen.
  Live geverifieerd: 4 correcties daadwerkelijk toegepast op artikel 1 IW 1990.
- graph-qa annotatie-critic — kwaliteitsoordeel over voorstellen (PR #86, story 048): tweede
  story van de annotatieketen-werkstroom. `critic_node` (losstaand, nog niet in `build_graph`
  gewired) beoordeelt `annoteer_node`'s voorstellen met een aandacht-niveau
  (groen/geel/rood) + actie (behoud/vervang/verwijder) per element, plus waarschijnlijk gemiste
  elementen. Interne element-ids in de motivatie worden vervangen door een kort citaat
  (`vervang_ids_door_citaat`). Bij een tweede beoordelingsronde dempt de node een eindoordeel dat
  de eigen, al uitgevoerde correctie terugdraait (`demp_zelfweerspreking`). Een mislukte
  Critic-call breekt de keten nooit. Live geverifieerd tegen artikel 1 van de Invorderingswet
  1990: de Critic ving een echte misclassificatie van de annotator met een correcte rode kaart
  en concrete correctie-suggestie.
- graph-qa dedup `komt_letterlijk_voor`/`normaliseer` (PR #85): zelf ontdekt na de merge van
  PR #84 dat story 047 deze functies opnieuw kopieerde in `agent/annotatie.py`, terwijl een
  vervolgpunt uit PR #81 al vastlegde dat ze naar een gedeelde plek moesten verhuizen zodra er
  een tweede consument kwam. Verhuisd naar nieuw `agent/brongetrouw.py`; `grounding.py` en
  `annotatie.py` importeren er nu allebei uit. Geen gedragswijziging.
- graph-qa annotatie — enkele ronde, geen critic (PR #84, story 047): eerste story van de
  annotatieketen-werkstroom. `annoteer_node` (losstaand, nog niet in `build_graph` gewired)
  classificeert één bepaling volgens het JAS in één LLM-call — brongetrouw en ontdubbeld via
  `sleutel_van`, met een balanced-braces-salvage voor een afgekapte/omkaderde LLM-respons.
  Twee nieuwe voorwaardelijke modules (`agent/graph/results.py` SPARQL-TSV-parser,
  `agent/artikel.py` corpus-tekst per bepaling) hergebruiken de bestaande SPARQL-bouwers uit
  story 041, geen nieuwe queries. `agent/jas_klassen.py` heeft een echte drift-guard-test tegen
  `api/references/jas-klassen-referentie.md`. Bewust nog geen critic/patch/herzie/emit/advance,
  kandidaten/ONDERWERP-afhandeling, jurist-marking-merge of integratie met
  `api/app/features/annotatie` — allemaal latere stories. Live geverifieerd tegen artikel 1 van
  de Invorderingswet 1990: 5 grounded voorstellen, 0 verworpen fragmenten, correcte ontdubbeling
  naar alternatieven bij genuine classificatie-ambiguïteit.
- graph-qa decompositie — multi-hop-graaf-variant (PR #83, story 046): tweede graaf-topologie
  (`decompose_node → solve_node → synthesize_node → verify_node → (resynth_node) →
  finalize_node`), selecteerbaar via `settings.enable_decomposition` (standaard uit — de
  bestaande agent⇄tools-lus uit stories 044-045 blijft dan byte voor byte ongewijzigd, bewezen
  doordat alle 136 bestaande tests ongewijzigd blijven slagen). Een samengestelde vraag wordt
  eerst in deelvragen gesplitst, elke deelvraag krijgt een eigen agent⇄tools-lus met een gedeelde
  `source_trace`, en de bevindingen worden samengevoegd tot één antwoord. `solve_node` is de
  eerste plek die de al bestaande prompt-cachingsplit (`ports.Systeem`, story 039) daadwerkelijk
  gebruikt. `synthesize_node`'s herkansing benoemt zowel `unsupported` als `niet_letterlijk`,
  dezelfde bugklasse-fix als `correct_node` op het legacy-pad. Live geverifieerd: een
  samengestelde vraag over twee begrippen wordt correct gesplitst en elk deel tegen het juiste
  onderdeel van artikel 2 lid 1 beantwoord (belastingschuldige → k, belastingaanslag → m), 27
  bronnen, gegrond.
- graph-qa supervisor — specialist-routing + afwijzen (PR #82, story 045): nieuwe
  `supervisor_node` vóór `agent_node` kiest een specialist (`definitie`/`duiding`/`algemeen`,
  elk met een eigen prompt-addendum en beperkte toolset via het al bestaande
  `anthropic_schemas(only=...)`) en wijst een vraag buiten de wetgeving direct af, zonder
  tool-/graafaanroep. Nieuwe modules `agent/{specialists,supervisor}.py`. Bewust geen
  worker-keten (antwoord+annotatie), geen apart routermodel, geen prompt-caching-split — zie
  story §Afwijkingen. Live geverifieerd: een begripsvraag routeert naar `definitie` met een
  letterlijk citaat uit art. 2 lid 1 onderdeel k, een structuurvraag naar `duiding`, een
  weervraag wordt afgewezen zonder de graaf te raken.
- graph-qa antwoord-agent-loop (PR #81, story 044): eerste werkende agent-loop (LangGraph) die de
  drie eerdere bouwstenen (LLM-adapter/039, GraphDB-adapter/040, toollaag/041) samenvoegt —
  `agent_node ⇄ tools_node → verify_node → (correct_node → agent_node | finalize_node)`, bewust
  zonder supervisor/annotatieketen/decompositie/checkpointer/streaming/API-laag (de kleinste
  zelfstandig bewijsbare snede, eerste story van de agent-loop-werkstroom). Nieuwe modules
  `agent/{models,provenance,grounding,prompts,orchestrator}.py`; `namespace.py` kreeg
  `vindplaats_patroon()` terug (story 041 liet 'm bewust weg zonder consument). Nieuwe
  dependency: `langgraph`. Live geverifieerd tegen de Invorderingswet-fixture: een gegrond
  antwoord met een correct letterlijk citaat en 40 reële bronnen.
- uitklapmenu-parity met wetsanalyse-ai (PR #80, story 043): sidebar-uitklapmenu 1:1 met de
  referentie — "Account & instellingen", "Beheer" (verhuisd uit de hoofdnavigatie, alleen
  beheerder), "Feedback geven" (nieuw menu-item), "Uitloggen"; trigger-knop toont nu ook een
  rol-onderschrift. `components/feedback/FeedbackKnop.tsx` (losse zwevende knop) vervalt ten
  gunste van `components/feedback/FeedbackDialoog.tsx` (zelfde formulierlogica, nieuwe schil via
  `components/ui/Dialog.tsx`, dat een `compact`-variant kreeg — de eerste echte tweede consument
  na story 042, die bewust alleen `center` bouwde). De hoofdnavigatie blijft projecten/
  werkplek-gericht: de referentie se chatgeschiedenis-sidebar bestaat hier nog niet (geen
  chat-backend/graph-qa-orkestrator), scope daarop afgestemd met de gebruiker.
- instellingenvenster (PR #79, story 042): Account + de 8 beheer-secties (incl. een negende,
  `berichten`-CRUD, ontdekt tijdens de bouw op het oude `app/beheer/page.tsx`-dashboard) verplaatst
  naar één gedeeld instellingenvenster — `lib/instellingen.ts` (tabtabel + padhelpers),
  `app/instellingen/[[...tab]]/page.tsx` (volle pagina) + `app/@modal/(.)instellingen/[[...tab]]/
  page.tsx` (intercepting-route-dialoog via een nieuw `modal`-parallel-route-slot in
  `app/layout.tsx`), `components/instellingen/{InstellingenInhoud,InstellingenDialog}.tsx`. Poort
  van `wetsanalyse-ai`'s `Dialog`/`Tabs`-componenten, bewust beperkt tot de varianten die hier al
  een consument hebben. Oude `/beheer`- en `/account`-routes blijven als kale redirects; de
  rol-gate in `auth.config.ts` verschuift van `/beheer` naar `/instellingen/beheer`. Twee
  regressies tijdens de bouw gevonden en gefixt via de volledige e2e-suite (zie de story): een
  gemiste negende tab, en een sessie-invalidatie-bug in de nieuwe rol-gate (dezelfde 401/403-les
  als story 038, herhaald op de pagina-laag).
- graph-qa toollaag (PR #78, story 041): getypeerde domein-toollaag over de kennisgraaf —
  `agent/namespace.py` (IRI-ruimte + drift-guard tegen `tools/bwb-import`), `agent/graph/
  queries.py` (11 geparametriseerde SPARQL-bouwers), `agent/graph/schema.py` (gecachete
  schema-introspectie), `agent/tools/__init__.py` (13-tool-registry + `dispatch()`). Poort van de
  `wetsanalyse-ai`-referentie, met drie schemacorrecties gevonden via live-verificatie tegen de
  lokale GraphDB (die referentie se importer-schema wijkt af van dit project se
  `tools/bwb-import`): `owl:sameAs` i.p.v. `bwb:jci`, `bwb:heeftOnderdeel+`/`eli:has_part` i.p.v.
  het niet-bestaande `bwb:bevat`, en een `STR()`-fix voor een stille SPARQL-`CONCAT()`-typefout.
  Nog geen aangesloten API/UI.
- graph-qa GraphDB-adapter (PR #77, story 040): tweede poort-implementatie — `MCPClient`
  (`GraphPort`) tegen de GraphDB MCP-server, allowlist-gebaseerd read-only-vangnet (SPARQL moet
  met SELECT/ASK/CONSTRUCT/DESCRIBE beginnen, ook na een `PREFIX` op dezelfde regel — niet een
  blocklist die dat miste). Poort van de `wetsanalyse-ai`-referentie (code + tests), grotendeels
  1:1. Tijdens live-verificatie tegen de lokale GraphDB 11.4.0 een echte afwijking gevonden: die
  versie weigert `tools/call` zonder eerst een MCP-sessie te openen via `initialize` — de
  referentie deed dat nergens expliciet; `_rpc()` doet dat nu lazy. Met story 039 zijn beide
  poorten uit story 029 nu ingevuld. Nog geen aangesloten API/UI.
- graph-qa LLM-adapter (PR #76, story 039): eerste echte `LLMPort`-implementatie voor
  `tools/graph-qa` — `AnthropicLLM` via `anthropic.AnthropicFoundry` (Anthropic-modellen op Azure
  AI Foundry), met prompt-caching (cache-punt op het stabiele systeemblok-deel) en terugval als
  de provider `cache_control` weigert. Poort van de `wetsanalyse-ai`-referentie-adapter, met één
  bewuste afwijking: de dedicated `AnthropicFoundry`-client i.p.v. de oudere
  `Anthropic(base_url=...)`-vorm. Correctie op story 029: `Settings.azure_foundry_base_url` (een
  volledige URL, klopte niet met de SDK) hernoemd naar `azure_foundry_resource`. Live
  geverifieerd tegen de echte Foundry-resource. Nog geen aangesloten API/UI — interne
  infrastructuur voor latere graph-qa-stories.
- BFF-rolautorisatie voor admin-routes (PR #75, story 038): geen enkele `/api/admin/*`-BFF-route
  (en `/api/projecten/[id]/llm-calls`) controleerde `session.user.rol` vóór het doorproxyen —
  project-breed gat sinds story 006, bewust opengelaten tijdens de vervolgpunten-sweep in PR #73.
  Nieuwe `requireBeheerder()`/`adminProxy()` in de BFF-laag (401 bij geen sessie, 403 bij een
  sessie zonder beheerder-rol — bewust onderscheiden, zie de story-doc voor een regressie die
  een blanket-403 opleverde), edge-redirect voor `/beheer`, sidebar verbergt de Beheer-link voor
  niet-beheerders. Lost tegelijk het al genoteerde DRY-vervolgpunt op (`requireSession`+
  `apiProxy`-boilerplate was gedupliceerd over 19 route-bestanden). Nieuwe
  `tests/e2e/rolautorisatie.spec.ts`.
- ADR-0011 SQLModel-migratie voor identiteit_toegang (PR #74): laatste feature die nog op
  SQLModel ORM draaide, nu SQLAlchemy Core + Pydantic (zelfde patroon als `api_tokens`) — Core
  `Table gebruikers`, niet-publiek `_GebruikerRij` + expliciete mappingfuncties
  (`gebruiker_uit_rij`/`naar_read`/`naar_mijn_profiel`), `engine.connect()`/`engine.begin()`
  i.p.v. `AsyncSession`. Geen gedragswijziging. `_is_laatste_actieve_beheerder` telt nu via
  `COUNT(*)`. `sqlmodel`-dependency verwijderd uit `pyproject.toml`. Sluit het laatste
  bewust-opengelaten punt uit PR #73's vervolgpunten-sweep af.
- Vervolgpunten-triage-sweep (PR #73): systematische triage van alle openstaande vervolgpunten — elk item geverifieerd, dan gefixt/vervallen-gemarkeerd/bewust opengelaten (volledige uitkomst per item in `docs/project/vervolgpunten.md`). Belangrijkste vondst: `api/app/engine/` en de rapport-bekijken-feature (story 013) bleken al verwijderd in PR #36; CLAUDE.md's "Gebouwde features" was daar niet op bijgewerkt, nu gecorrigeerd. Gefixt: `api_tokens.label` `max_length`; `GebruikerCreate`/`GebruikerPatch.rol` naar `Literal[...]` (handmatige 422-check overbodig, verwijderd); `GebruikerPatch` weigert een lege body; LaatsteBeheerder-query gededupliceerd; `runtime_config`'s cache getypeerd + inline imports naar module-niveau; `WetCreate.bwb_id` (nooit gelezen) verwijderd + frontend meegewerkt; BewerkenFormulier-crash bij verwijderen-tijdens-bewerken gefixt (nieuwe e2e-dekking, `wetten-beheer.spec.ts`, deze pagina had nog geen enkele test); `httpx.ProtocolError` toegevoegd aan except-tuples in `wettenbank.py`/`annotatie/graphdb.py`; frontend `formatDatum`-dedup, `use(params)`-migratie (React 19), disclaimer-cookie `secure`-flag, `setTimeout`-cleanup (2x), onbenutte BFF-route verwijderd. Bewust opengelaten: ADR-0011 SQLModel-migratie, twee race conditions (nu relevant sinds Postgres-only, verdienen een gezamenlijke oplossing), project-brede BFF-rolautorisatie, `structuur()`'s hardgecodeerde placeholder (nu haalbaar via GraphDB, zelfde patroon als story 037 — eigen story).
- `crypto.py` FERNET_KEY_FILE (PR #72): de Fernet-master-key wordt nu gelezen via `FERNET_KEY_FILE` i.p.v. de platte `FERNET_KEY`-env-var (werkwijze-ADR-0006) — sluit een vervolgpunt uit story 024. CI-workflow (`test-frontend-e2e`) en test-fixtures (`test_2fa.py`, `test_llm_profielen.py`) meegewerkt.
- Httpx-client-hergebruik in `wettenbank.py` (PR #71): `_jsonrpc_call` gebruikt nu een proces-brede, lazily aangemaakte `httpx.AsyncClient` i.p.v. een nieuwe per aanvraag — sluit twee langer openstaande vervolgpunten af (PR #15 story 020, PR #17 story 024). Module kreeg zijn eerste directe testdekking (7 tests, `httpx.MockTransport`).
- Werkplek — onderdelen + parity-fixes voor wetsartikeltekst (PR #70, story 037): onderdelen (a/b/c-opsommingen) onder een lid en direct onder een artikel zonder leden, numerieke lid-sortering (`_sorteersleutel`), en een bepaling-fallback voor decimale circulaire-nummers toegevoegd aan `GET /v1/annotatie/documenten/{slug}/wetsartikel` — gebaseerd op vergelijking met wetsanalyse-ai's `graph-qa`-agent (`artikel.py`/`queries.py`). Live geverifieerd: Invorderingswet art. 2 lid 1 toont nu 24 onderdelen (voorheen 0).
- Simplify-sweep over stories 030-037 (PR #69): retroactieve `/simplify`-ronde (Simplify-instelling stond op `nee` tijdens die hele reeks) — `GRAPHDB_PASSWORD_FILE` in `api/app/features/annotatie/graphdb.py` wordt nu één keer bij module-load gelezen i.p.v. per aanvraag, plus een proces-brede `httpx.AsyncClient` i.p.v. een nieuwe per aanvraag; `tools/bwb-import/app/collect.py` kreeg een `_provenance_props()`-helper die de 6 identieke provenance-velden dedupliceert over `_artikelen`/`_bijlagen`/`_divisies` (RDF-output bewijsbaar ongewijzigd); dode `datum=None`-kwarg verwijderd in `parser.py`. Frontend-hook-extractie en twee kleine error-mapping-duplicaten bewust overgeslagen (risico/indirectie woog niet op tegen de winst).
- Werkplek — echte wetsartikeltekst via GraphDB (PR #68, story 037): nieuw GraphDB-SPARQL-leesendpoint (`GET /v1/annotatie/documenten/{slug}/wetsartikel`, `api/app/features/annotatie/graphdb.py`) toont echte wetsartikel-/ledentekst in `/werkplek/{slug}` i.p.v. de placeholder; sluit story 023's laatste open acceptatiecriterium. Twee bugs pas bij handmatige verificatie tegen de live GraphDB ontdekt en in dezelfde PR gefixt: ontbrekende `GRAPH ?g`-wrapper (data staat per wet in een eigen named graph) en een verkeerde aanname dat `bwb:tekst` altijd op de Artikel-node zelf staat (niet zo bij een artikel met leden). Geen `shared/`-plek voor de SPARQL-client — precies één consument.
- Tekstuele fallback-verwijzingsdetectie voor bwb-import (PR #67, story 036): poort van `wetsanalyse-ai`'s `afkortingen.py` + `detect_textual_references`, na afstemming met de gebruiker over de conservatieve aanpak (kleine hardcoded afkortingentabel + één regex, onbekende afkorting altijd overgeslagen — nooit gegokt). Elke treffer draagt `soort=tekstueel`/`betrouwbaarheid=laag`, nooit vermengd met structured refs; overgeslagen bij een al-gestructureerd artikelnummer. Volledig uitzetbaar via `BWB_DETECT_TEKSTUELE_REFS`. Sluit de volledige scope uit story 027 §Buiten scope af. Handmatig geverifieerd: 22 echte, correct opgeloste verwijzingen gevonden in de Invorderingswet-fixture tegen de lokale, gelicentieerde GraphDB.
- Lucene-FTS-connector voor bwb-import (PR #66, story 035): poort van `wetsanalyse-ai`'s `ensure_fts_connector` + helpers. Waarborgt GraphDB's ingebouwde Lucene-index over de BWB-tekstvelden als onderdeel van `prepare()`, zelfherstellend en idempotent — onleesbare/verouderde config wordt nooit stilzwijgend als actueel aangenomen. Sluit de kernscope uit story 027 §Buiten scope af, op tekstuele verwijzingsdetectie (036) na. Handmatig functioneel geverifieerd tegen de lokale, gelicentieerde GraphDB (echte, gerankte zoekresultaten).
- Circulaires (divisies) voor bwb-import (PR #65, story 034): poort van `wetsanalyse-ai`'s `_parse_divisie`/`_divisie_tekst`. Circulaires/beleidsregels dragen geen wettekst maar een recursieve `<circulaire.divisie>`-boom; `parse()` gooit hiervoor niet langer een `ParseError` maar bouwt de divisie-boom op (container + tekstdrager, citeerbaar, `VOLGT_OP`-documentvolgorde net als bijlagen in story 033). Bewuste afwijking van de referentie: het écht-onherkende restgeval (noch wettekst, noch circulaire) blijft een harde `ParseError` (brongetrouwheidsprincipe uit `tools/bwb-import/CLAUDE.md`); `_divisie_tekst` rendert — anders dan de referentie — ook tabellen, consistent met `_lichaamstekst`/`_bijlage_tekst`. Hiermee is de kernscope uit story 027 §Buiten scope afgerond op de Lucene-FTS-connector en tekstuele verwijzingsdetectie na. Handmatig geverifieerd tegen de lokale, gelicentieerde GraphDB met een synthetische `Wet`.
- Bijlagen voor bwb-import (PR #64, story 033): poort van `wetsanalyse-ai`'s `_parse_bijlage`/`_bijlage_tekst`. Een bijlage (kind van `<wet-besluit>`/`<regeling>`, ná de wettekst) is container (eigen artikelen/onderdelen) én tekstdrager, citeerbaar op JuriConnect-sleutel net als een artikel. Hergebruikt de bestaande provenance-/voetnoot-/definitie-/illustratie-/tabel-logica uit story 031 en de artikel-traversal in `collect.py`. Eerste documentvolgorde-relatie in het project (`VOLGT_OP` tussen opeenvolgende bijlagen). Handmatig geverifieerd tegen de lokale, gelicentieerde GraphDB met een synthetische `Wet` (de Invorderingswet-fixture bevat geen bijlage).
- Wet-brondata/aanhef/considerans/ondertekenaars voor bwb-import (PR #63, story 032): poort van `wetsanalyse-ai`'s `_wet_brondata`/`_wet_aanhef`/`_parse_ondertekenaars`. `Wet` draagt nu publicatiebron (Stb-jaar/nummer, ondertekenings-/uitgiftedatum, Kamerdossier), aanhef/considerans, toestand-URL (`bwb-ng-vast-deel`) en ondertekenaars. Ondertekenaars zijn wet-overstijgende, open-world-gededupliceerde nodes (zelfde patroon als de WTI-Organisatie-node uit story 030). `collect.py` uitgebreid om de brondata-velden op de Regeling-node-props te zetten. Handmatig geverifieerd tegen de lokale, gelicentieerde GraphDB.
- Artikel/lid/onderdeel-verrijking voor bwb-import (PR #62, story 031): poort van `wetsanalyse-ai`'s provenance-attributen (inwerking/bron/effect/status/terugwerkend_tot/wijzigingsbronnen), voetnoten, cursief-gedefinieerde begrippen en illustraties naar `parser.py`/`collect.py`/`ontology.py`/`models.py`. CALS-tabellen worden niet langer uitgesloten van de lopende tekst maar als leesbare `cel | cel`-rijen gerenderd (`_tabel_tekst`). Nieuwe `Illustratie`-node + `BEVAT_ILLUSTRATIE`-relatie loopt mee via de bestaande generieke node-/rel-schrijflus in `graphdb_writer.py` (geen signatuurwijziging). Handmatig geverifieerd tegen de lokale, gelicentieerde GraphDB.
- WTI-verrijking voor bwb-import (PR #61, story 030): poort van `wetsanalyse-ai`'s `wti_parser.py` + WTI-secties (downloader/config/main/graphdb_writer/ontology/rdf_vocab) naar `tools/bwb-import`, aangepast aan dit projects eenvoudigere node-schema (geen `iri_soort`/`iri_sleutel`). Nieuwe `WtiParser`/`WtiInfo`/`WtiElementRel`, `Vocab.begrip()`/`Vocab.entiteit()`, ontologie-uitbreiding (`Organisatie`-klasse, `heeftGrondslag`/`uitgegevenDoor`/`inFamilie`/`grondslagVoor`/`bevoegdheidVoor`/`verwijzingDoor`), `label_id`-join-sleutel op `Wet`/`Structuurdeel`/`Artikel`. Opt-in via `BWB_IMPORT_WTI` (default uit), best-effort (een falende WTI-stap breekt de kernwet-import nooit). Handmatig geverifieerd tegen de lokale, gelicentieerde GraphDB.
- docs(c4-model): bijwerken t/m PR #27 — L2-container-tekst voor `api` en `frontend` reflecteert nu de gebouwde stand; L3-`api` uitgebreid met alle features (annotatie, api_tokens, llm_calls, llm_profielen, projecten, runtime_config, wetcatalogus), engine-module en shared-modules (wettenbank, llm, db, crypto, validation, tijd) inclusief relaties; L3-`frontend` uitgebreid met alle pagina's (account, setup, beheer-subpagina's, wetcatalogus, projecten/detail, werkplek/lijst+detail, disclaimer) en de `apiProxy` + BFF-groep. Adresseert de 8 opgestapelde c4-model-vervolgpunten uit PRs #9, #10, #11, #17, #18, #21, #22, #27.
- refactor(llm_calls): verhuizen naar eigen feature-map (PR #27): `llm_calls`-tabel, `LlmCallRead` en `SqlAlchemyLlmCallsStore` verplaatst uit `projecten/` naar de nieuwe `api/app/features/llm_calls/`-feature (models/store/dependencies/tests), met eigen `MetaData()`-instantie. Endpoint `GET /v1/projecten/{analyse_id}/llm-calls` blijft in projecten (path-scoped resource) maar importeert `LlmCallRead` + `SqlAlchemyLlmCallsStore` + `get_llm_calls_store` uit de nieuwe locatie; `engine/orchestrator.py` idem (lazy import weg — geen cycle meer). `alembic/env.py` neemt `llm_calls_metadata` op in de autogenerate-lijst. Adresseert punt 4 uit architectuur-audit ronde 2. Simplify: `get_llm_calls_store` naar `llm_calls/dependencies.py` (weg uit projecten's router-owner-export), lazy import in orchestrator opgeheven, ongebruikte `Integer`-import uit `projecten/models.py` verwijderd, `AsyncEngine`-typing in `SqlAlchemyLlmCallsStore.__init__`.
- refactor(auth): DB-verify verhuizen naar api_tokens (owner-export) (PR #26): shared/auth.py roept nu de nieuwe publieke owner-export verifieer_db_token uit api_tokens.store aan; lazy import + try/except in shared/ verwijderd, boundary shared/ → features/ hersteld (audit ronde 2, punt 3).
- Shared upsert-helper voor berichten en runtime_config (PR #24): Ontdubbelt de dialect-aware upsert-boilerplate (`pg_insert if is_pg else sqlite_insert`) naar `api/app/shared/db.py` met twee helpers — `dialect_insert(engine_or_conn, table)` voor callers die zelf `.from_select()`/`.on_conflict_*()` chainen (berichten.markeer_alles_gelezen) en `upsert(engine_or_conn, table, values, conflict_cols, update_cols=None)` voor de complete upsert (runtime_config.schrijf, wetcatalogus.upsert). `wetcatalogus.upsert` gaat tegelijk van check-then-insert (2 queries, TOCTOU-race op Postgres) naar één atomaire upsert met `RETURNING` — lost MEDIUM-vervolgpunt uit PR #15 op. 5 unit-tests in `api/app/shared/tests/test_db.py` dekken insert/update-pad + dialectkeuze. Adresseert punt 2 uit architectuur-audit ronde 2.
- Wettenbank-MCP JSON-RPC-consolidatie (PR #23): Ontdubbelt de JSON-RPC POST-boilerplate tussen `shared/wettenbank.haal_artikel_op` en `wetcatalogus/router._roep_wettenbank_mcp_aan`. Nieuwe private `_jsonrpc_call(method, arguments, foutcontext)` doet de gedeelde plumbing (payload, POST, netwerkfouten, HTTP-status, JSON-RPC-error, content-blokken). Nieuwe publieke `haal_citeertitel_op(bwb_id) -> str` (tool `wettenbank_structuur`). Twee subklassen `WettenbankNietBereikbaar` en `WettenbankNietGevonden` zodat de router 502/404 kan blijven onderscheiden. Router-boilerplate (~75 regels) verwijderd. Adresseert punt 1 uit architectuur-audit ronde 2.
- Werkplek annotatie-UI (PR #22, story 023): BFF-proxy-routes voor alle annotatie-endpoints van story 022 (documenten GET/POST/DELETE, elementen PUT, beslissing POST, auditlog GET) via `apiProxy()` + `requireSession()`; werkplek-pagina `/werkplek/` (documentenlijst met status-badges, aanmaakformulier, `VerwijderKnop`); documentdetailpagina `/werkplek/{slug}` met twee-kolommen-layout (wetsartikeltekst-placeholder links, elementenlijst rechts), beslissingsacties (goedkeuren/bewerken/afwijzen) met verplichte reden-select, auditlog-tabblad; `NavigatieHeader.tsx` uitgebreid met Werkplek-link; `generated/types.ts` geregenereerd inclusief annotatie-types; Playwright E2E-tests voor lijst, aanmaken, beslissingen, auditlog en verwijderflow. Simplify: `formatDatum` geëxtraheerd naar `lib/datum.ts`; `VerwijderKnop` hergebruikt i.p.v. inline `confirm()`; unified `AANDACHT_STIJL`-lookup; `useMemo` voor sortering; redundante guard en dode stijl verwijderd; 401-redirect op alle mutaties.
- Annotatie-backend (PR #21, story 022): Migratie 0011 (`annotatie_documenten` + `annotatie_audit`), 7 endpoints onder `/v1/annotatie/documenten` (aanmaken, lijst, ophalen, verwijderen, bulk PUT elementen, beslissing registreren, auditlog opvragen), client-scoping via `X-User-Id` (andermans document → 404), JAS-klassevalidatie + lege-tekst-filter bij PUT (aanvaard/verworpen-count in response), levenscyclus- en documentstatus-herberekening na beslissing, append-only auditlog, 19 gedragstests. `GELDIGE_JAS_KLASSEN` verplaatst van `engine/validation.py` naar `shared/validation.py` (tweede onafhankelijke gebruiker; `engine/validation.py` herexporteert voor backwards-compatibility).
- LLM-calls log (PR #20, story 021): Voegt `GET /v1/projecten/{analyse_id}/llm-calls` toe + `SqlAlchemyLlmCallsStore.lijst_calls()` + admin-pagina `/beheer/llm-calls` voor het bekijken van vastgelegde LLM-aanroepen per analyse; BFF-route `frontend/app/api/projecten/[id]/llm-calls/route.ts`; Playwright E2E-test (`llm-calls.spec.ts`, 3 scenario's); 5 pytest-integratietests.
- API-tokens (PR #18, story 018): Voegt `api/app/features/api_tokens/` toe — SQLAlchemy Core-tabel, Protocol+SqlAlchemy-store, drie admin-endpoints (`GET`/`POST`/`DELETE /v1/admin/api-tokens`), Alembic-migratie 0010. `shared/auth.py` async gemaakt en uitgebreid met DB-tokenverificatielaag (SHA-256, `hmac.compare_digest` voor statisch token). Frontend: `/beheer/api-tokens`-pagina met eenmalig-token-modal en `/beheer`-badge met actief-tokenaantal.
- Rapport bekijken (PR #19, story 013): `GET /v1/projecten/{id}/rapport` (JSON) + `GET /v1/projecten/{id}/rapport.md` (Markdown-download met `Content-Disposition`-header); `rapport_md.py` defensieve Markdown-renderer (werkgebied, bronnen, begrippen, afleidingsregels); frontend-pagina `/projecten/{id}/rapport` met bronnen-kaarten, begrippen-tabel en afleidingsregels-lijst, teruglink + download-knop; 409-melding als analyse nog niet klaar; `apiProxy` uitgebreid met optionele `forwardHeaders`-parameter; Playwright E2E-test (409 + 404); 7 gedragstests.
- Analyse-engine (PR #17, story 024): `api/app/engine/` (orchestrator act2→human-in-the-loop→act3, steps, prompts, retry met exponential backoff), `api/app/shared/llm/` (LLMClient Protocol + LiteLLMClient met JSON-parse-herpoging), `api/app/shared/wettenbank.py` (JSON-RPC POST naar Wettenbank-MCP), `api/app/shared/validation.py` (JAS-klasscheck + brongetrouwheid NFKC-substring), migratie 0009 (`analyses.rapport JSON nullable` + `llm_calls`-tabel), nieuwe endpoints `POST /v1/projecten/{id}/akkoord` + `POST /v1/projecten/{id}/afwijzen` (human-in-the-loop signalering), `api/references/` (jas-klassen-referentie + begrippen), LLM-capture via `sla_llm_call_op` + `capture_llm_calls` runtime_config-toggle, twee nieuwe store-methoden (`haal_rij_op_naam` + `haal_standaard_rij`) op `llm_profielen`; 157 tests groen, ruff clean.
- Initiële API-features (PR #1, stories 001-002): `api/app/features/feedback/` (indienen, admin-lijst, verwijderen, ongelezen-aantal, markeer-gezien) en `api/app/features/berichten/` (aanmaken, bewerken, publiceren/depubliceren, verwijderen admin; lezen, ongelezen-status, lees-alles analist); `api/app/features/identiteit_toegang/` (eigen gebruikers-tabel, bcrypt, POST /v1/auth/verify); SQLAlchemy Core + Pydantic als "ene bron", Alembic-migraties, pytest-integratietests.
- Eerste frontend: berichten-admin-scherm (PR #2): Eerste frontend-service (`frontend/`) — Next.js admin-scherm voor berichtenbeheer, contractgeneratie API→TypeScript via `openapi-typescript`, CORS-middleware op de API, Playwright E2E-test (gelukkig pad + foutpad), eigen `frontend-ci.yml`, C4-architectuurdocumentatie.
- Huisstijl-frontend (PR #3): Wetsanalyse-kleurtokens als CSS-variabelen in `globals.css`, Fira Sans via `next/font/google`, navigatieheader in `layout.tsx`, en herstructurering van `page.tsx` naar CSS-klassen (`btn`, `field-input`, `tabel`, `badge`, `melding`); E2E-selectorfix `p[role="alert"]` in commit `9c45f09`.
- Auth — Keycloak OIDC-login (PR #4, story 005): Keycloak Authorization Code + PKCE-flow in de frontend (SubtleCrypto, localStorage), JWT-verificatie via JWKS in de API (RS256, cache TTL 300s, asyncio.Lock), `shared/auth.py` vervangt X-Admin-Id header-stand-in door `huidige_beheerder`-dependency met `GebruikerContext`, Playwright E2E via ROPC + `page.addInitScript`, realm-export + CI-opstapstap voor Keycloak 26.
- Auth — eigen gebruikers (PR #5, story 006): Vervangt Keycloak OIDC door eigen `gebruikers`-tabel (bcrypt, SQLModel, Alembic-migratie 0003), `POST /v1/auth/verify` achter API_TOKEN-gate met constant-time vergelijking en timing-oracle-bescherming, Auth.js v5 Credentials provider met httpOnly-sessiecookie, Next.js BFF-routes voor admin-endpoints (`/api/admin/berichten/...`), Keycloak volledig verwijderd uit docker-compose en CI.
- Disclaimer-gate + PoC-strip (PR #7, story 008): server-side middleware-redirect op basis van httpOnly-cookie (`disclaimer_geaccepteerd`), `/disclaimer`-pagina met drie waarschuwingsblokken, PoC-strip in `layout.tsx`, `isDisclaimerExempt()` parallelt `isPublic()`; open-redirect-fix (route geeft gesaniteerde redirect-URL terug); simplify: `TYPE_META`/`TypeBadge`/`SectieHeader`/`LeegePlaceholder` geëxtraheerd naar gedeelde modules, mutatie-state in `beheer/page.tsx` werkt lokaal bij zonder extra GET; favicon + `manifest.webmanifest` toegevoegd.
- Feedback-frontend (PR #8, story 009): zwevende `FeedbackKnop` in `layout.tsx` voor alle ingelogde gebruikers; 5 BFF-routes (`POST /api/feedback`, GET/DELETE/markeer-gezien/ongelezen-aantal voor admin); admin-feedbackpagina (`/beheer/feedback`) met verwijderen + markeer-gezien op mount + 404-race-afhandeling; navigatieknop met ongelezen-badge op `/beheer`. Gedeelde modules: `lib/feedback-types.ts`, `lib/beheer-fetch.ts` (typed `BeheerFetchFout`, vervangt duplicaat in `beheer/page.tsx`), `components/feedback/CategorieBadge.tsx`, `components/feedback/FeedbackItem.tsx`.
- LLM-profielen (PR #10, story 011): `LlmProfielenStore` (Protocol + SQLAlchemy-implementatie) + `shared/crypto.py` (Fernet-encryptie, fail-closed); CRUD-endpoints `GET/POST /v1/admin/profielen` + `PUT/DELETE /v1/admin/profielen/{naam}` achter `huidige_beheerder`; Alembic-migratie `0004_llm_profielen_tabel`; BFF-routes `app/api/admin/profielen/`; beheer-UI `/beheer/llm-profielen` (tabel + aanmaken-formulier + bewerk-/verwijder-actie per profiel); 15 integratietests + 3 Playwright E2E-scenario's; mockup-pagina gepromoveerd en verwijderd.
- Wetcatalogus (PR #9, story 010): Voegt wetcatalogus-feature toe aan `api` (`models.py`, `store.py`, `router.py`, 7 gedragstests; endpoints `GET /v1/wetten` + `GET /v1/wetten/{bwb_id}/structuur`, statische PoC-data zonder DB-tabel) en `frontend` (BFF-routes `app/api/wetten/` en `app/api/wetten/[bwbId]/structuur/`, herbruikbaar `WetSelector`-component met wet- + artikelkeuze, pagina `app/wetcatalogus/`, E2E-test gelukkig pad + foutpad); contractgeneratie bijgewerkt (`openapi.json`, `types.ts`); mockup-pagina gepromoveerd.
- Analyse aanmaken & volgen + SSE (PR #11, story 012): `api/app/features/projecten/` — SQLAlchemy Core `analyses`-tabel, Protocol-store (`AnalyseStore`/`SqlAlchemyAnalyseStore`), CRUD-endpoints + SSE-stroom (`GET /v1/projecten/{id}/events`) + BackgroundTasks-PoC-job (statusovergangen wachtrij→actief→review/klaar/fout), Alembic-migratie 0005, 15 pytest-integratietests; BFF-routes `app/api/projecten/` (GET/POST, GET/DELETE/{id}, GET/{id}/events/), pagina's `/projecten`, `/projecten/nieuw`, `/projecten/[id]`, gedeelde `StatusPill`/`VerwijderKnop`-components (`components/projecten/`), Playwright E2E-test (gelukkig pad + validatiefout), Tailwind CSS + PostCSS + autoprefixer toegevoegd; mockup `/mockup/analyse/` verwijderd (gepromoveerd); `buildBackendHeaders` geëxporteerd vanuit `lib/api-client.ts`.
- Setup-flow initiële beheerder (PR #13, story 015): `GET /v1/auth/setup-status` + `POST /v1/auth/setup` achter `vereist_api_token`; Alembic-migratie 0006 (`email TEXT NOT NULL DEFAULT ''` op `gebruikers`); `SetupStatus`/`SetupVerzoek`/`GebruikerInfo` Pydantic-modellen met gebruikersnaam-regex-validatie; frontend `/setup`-pagina met `SetupFormulier` (client component) + BFF-routes via `publiekApiProxy`; `lib/setup-status.ts` roept backend rechtstreeks aan (geen loopback); `auth.config.ts` maakt `/setup` publiek en voegt redirect-logica toe (ingelogd → `/`, setup al klaar → `/login`); E2E-test voor gelukkig en foutpad; mockup-pagina gepromoveerd.
- Account-pagina (PR #12, story 016): Voegt GET /v1/auth/me en POST /v1/auth/wijzig-wachtwoord toe aan de identiteit_toegang-feature (MijnProfiel + WachtwoordWijzigenVerzoek Pydantic-modellen, domain-exceptions GebruikerNietActief/WachtwoordOnjuist, bcrypt buiten de DB-sessie); BFF-routes app/api/auth/me en wijzig-wachtwoord; account-pagina (/account) met profielweergave en wachtwoord-formulier (inline foutmelding per veld, formulier-reset na succes); Account-link in NavigatieHeader geactiveerd; E2E-test (4 scenario's); auth-testfixture hersteld naar tmp SQLite-DB (fix pre-existing + nieuwe CI-fout).
- Gebruikersbeheer uitbreiden (PR #14, story 014): `admin_router` met GET/POST `/v1/admin/gebruikers`, PATCH/DELETE `/v1/admin/gebruikers/{naam}`, POST `/v1/admin/gebruikers/{naam}/reset-wachtwoord` (achter `huidige_beheerder`); LaatsteBeheerder-invariant (409 bij deactiveren/degraderen/verwijderen van de enige actieve beheerder); `GebruikerRead`, `GebruikerCreate`, `GebruikerPatch`, `TijdelijkWachtwoord` in `openapi.json` en `types.ts`; BFF-routes `app/api/admin/gebruikers/`; `/beheer/gebruikers`-pagina (gepromoveerd van mockup) + navigatieknop op `/beheer`; E2E-test gelukkig pad + foutpad (laatste beheerder 409); `maak_gebruiker_admin`-TOCTOU-fix (check+insert in één transactie).
- Wettenbank-beheer (PR #15, story 020): Vervangt `HardgecodeerdeWetcatalogusStore` door `DatabaseWetcatalogusStore` (SQLAlchemy Core) met Alembic-migratie 0007, voegt admin-CRUD-endpoints toe voor de wetcatalogus (GET lijst, PUT upsert, DELETE, POST resolve via httpx naar Wettenbank-MCP) en koppelt een beheerpagina `/beheer/wetten/` met inline bewerken, resolve-knop en verwijderen met bevestiging; contractgeneratie bijgewerkt (`openapi.json`, `types.ts`); seed van de 3 bestaande hardgecodeerde wetten in de migratie.
- App-instellingen / runtime-config (PR #16, story 019): nieuwe feature `api/app/features/runtime_config/` met tabel `app_instellingen` (migratie 0008), in-process TTL-cache (10s), `GET/PUT /v1/admin/instellingen` (beheerder-auth); dialect-aware upsert (sqlite + postgresql); frontend `/beheer/instellingen/` met capture-LLM-calls-toggle + statusbadge; mockup-pagina gepromoveerd en verwijderd; contractgeneratie bijgewerkt (`openapi.json`, `types.ts`).
- Admin-MCP (PR #6, story 007): Nieuwe TypeScript stdio MCP-server (`tools/wetsanalyse-admin-mcp/`) met vier tools (`list_berichten_admin`, `maak_bericht`, `update_bericht`, `publiceer_bericht`) die de lexplainables admin-API aansturen; auth via `Authorization: Bearer` + `X-User-Id`; vier integratietests met gemockte fetch; `dist/index.js` meegecommit; registreerbaar in `.mcp.json`.
- graph-qa: checkpointer — gespreksgeheugen (PR #88, story 050): LangGraph-checkpointing (`agent/checkpointer.py`, Postgres → SQLite-bestand → `MemorySaver`), `build_graph(..., checkpointer=None)` en `nieuwe_beurt_invoer()` voor het per-beurt-resetpatroon; zelf gevonden en gefixt: `finalize_node`-Source-serialisatie (`.model_dump()`) en `supervisor_node` las de gesprekshistorie niet mee (`_recent_context(state)`).
- graph-qa: streaming — SSE-events (PR #89, story 051): `agent_node`/`synthesize_node` streamen hun eind-antwoord token-voor-token (`llm.stream()` + `get_stream_writer()`, gedeelde `_stream_final`-helper); nieuwe wrapper `agent/agent.py`'s `answer_stream()` levert het SSE-event-contract (`token`/`sources`/`grounding`/`conversation_id`/`done`/`error`) als async generator, nog zonder HTTP-laag.
- graph-qa: run/stop — stop_check/BeurtGestopt (PR #90, story 052): `build_graph(..., stop_check=...)` wikkelt elke node-registratie in een `stopbaar()`-wrapper die op een nodegrens `BeurtGestopt` (nieuwe `agent/agent_common.py`) gooit i.p.v. de node te draaien; `answer_stream()` geeft `stop_check` door en vangt `BeurtGestopt` als een normale afronding. Nog geen aanroeper — infrastructuur voor het latere runs-model.
- graph-qa: HTTP-laag — POST /v1/chat (PR #91, story 053): `tools/graph-qa/api/main.py` (was een leeg skelet) krijgt `GET /health` + `POST /v1/chat` (SSE via sse-starlette, gekoppeld aan de verbinding), providers via FastAPI `Depends()`, optioneel bearer-token (`QA_API_TOKEN`, timing-safe), lifespan fail-fast op ontbrekende graaf-/LLM-config. Nieuw `ChatRequest`-model; grounding-event kreeg `cited`/`niet_letterlijk` erbij.
